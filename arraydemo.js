let values=[1,2,3,4,5,6,4,5]
console.log(typeof values)
for(let i=0;i<values.length;i++)
    {
        console.log(values[i])

    }
    //console.log(i)
    //here let is a block scope variable
    //instead of let if we use var
    //we can get the output of i